[model]
type = "sentiment-analysis"
algorithm = "logistic-regression"  # Simple classifier for MVP
vocab_size = 10000
embedding_dim = 100

[inference]
confidence_threshold = 0.5
max_length = 512

[logging]
level = "INFO"  # DEBUG, INFO, WARNING, ERROR, CRITICAL

[server]
# Future: HTTP API configuration (v0.2.0)
host = "0.0.0.0"
port = 8080
workers = 4
